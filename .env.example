# Database
DATABASE_URL="postgresql://user:password@localhost:5432/manila_watch_atelier"

# Resend (for inquiry notification emails)
RESEND_API_KEY="re_..."

# App Configuration
APP_URL="http://localhost:3000"
ADMIN_EMAIL="sherard@manilawatch.com"

# Admin Authentication (SHA-256 hash)
# Generate hash: node -e "const c=require('crypto');console.log(c.createHash('sha256').update('YOUR_PASSWORD'+('manila-watch-salt')).digest('hex'))"
ADMIN_USERNAME="admin"
ADMIN_PASSWORD_HASH="your_hashed_password_here"
SALT="manila-watch-salt"
